version: '3.3'
services:
  dev-cdm:
    hostname: dev-cdm
    container_name: "MOS-CDM-COMPOSE"
    image: "cdm:v1.0"
    # restart: unless-stopped

    ports:
       - "48011:48011"
    volumes:
      - /home/shifter/david_tmp/log/:/home/cdmmgr/log
